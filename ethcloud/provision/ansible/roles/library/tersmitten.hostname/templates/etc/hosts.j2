# {{ ansible_managed }}

127.0.0.1 localhost.localdomain localhost
127.0.1.1 {{ hostname_hostname }} {{ hostname_hostname_short }}

# The following lines are desirable for IPv6 capable hosts
::1 ip6-localhost ip6-loopback
fe00::0 ip6-localnet
ff00::0 ip6-mcastprefix
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters
ff02::3 ip6-allhosts

{% for additional_host in hostname_additional_hosts %}
{% if additional_host['hostname_short'] is defined %}
{{ additional_host['ip_address'] }} {{ additional_host['hostname'] }} {{ additional_host['hostname_short'] }}
{% else %}
{{ additional_host['ip_address'] }} {{ additional_host['hostname'] }}
{% endif %}
{% endfor %}
